# Experience Candidate 1 — retained local evidence

These receipts are measurements of the carried Playthings source candidate, not product acceptance. The native Evidence and Handoff explain custody, source identity, required context and scope.

- `qualification.txt`: actual final `npm run check` (219 runtime + 22 real carried Core/App + 7 release policy cases, installed Playthings consumer).
- `package-qualification.json`: extracted verbatim JSON from that gate. `published:false` concerns this test operation, not Sigma's independent registry state.
- `installed-context.json`: real offline tarballs of carried Core/App and byte comparison to installed modules; no mock or symlink.
- `native-browser.json`: 11 real Chromium checks of decoding, cropping and native primitives using in-memory ES-module source. **React and App were not mounted.**
- `native-browser-primitives.png`: diagnostic colored test cells and receipt display, **not a Playthings UI preview or approved production art**.
- `native-browser-http-blocked.json`: earlier HTTP attempt denied by administrator policy; retained as a failure of that transport. No policy was changed.
- `registry-blocker.txt`: pinned React acquisition DNS blocker. No replacement React/Vite package was installed.
- `syntax.json`: syntax only, including four React ESM files; no UI correctness implication.
- `graphics-regression.json`: actual existing graphics-tool regression.
- `stress-metrics.json`: one authorless 5,000-artifact headless measurement; no FPS/general performance claim.
- `sibling-byte-audit.json`: all files in Business/Docs/Core/App/Site remain identical to their carried snapshots.
- `source-bindings.json`: final local source hashes and changed-file inventory, excluding receipts/runtime-only directories and Python caches; existing .release outputs in the inventory are not promised handoff content and the subsequently authored native Evidence/Handoff. Their exact integrity is handled by Tiinex.
- `qualification-summary.json`: bounded roll-up; raw receipts retain precedence over an incorrectly transcribed summary.

**Open gates:** actual Turn 2, host footer/chrome hiding and focus/scroll restoration, real lazy-loaded React/Vite/source-set browser test with corrected host-owned assertions, source-neutral dynamic schema-owner/subject bindings and provider-revision notification, final graphics approval and Sigma experience acceptance. The host/test gap is substantive, not solely an environment issue.

No immediate Sigma commit/push/test or npm publication is requested. The next action is to integrate the actual Turn-2 release in its owning lane. Do not apply the carried sibling snapshots over newer checkouts.
